16. Reverse an array in place (without using arr[::-1] or .reverse() — swap elements manually using two pointers, one from start, one from end)
17. Given an array, move all zeros to the end while keeping the order of other elements (e.g. [0,1,0,3,12] → [1,3,12,0,0])
Two-pointer pattern (very common in LeetCode):
18. Check if an array is a palindrome (reads the same forwards and backwards) — use two pointers moving toward each other
19. Given a sorted array and a target sum, find two numbers that add up to the target (two-pointer approach, not brute force nested loop)
Sliding window intro:
20. Find the maximum sum of any 3 consecutive elements in an array (this introduces the idea of a "window" moving through the array)
Frequency / duplicates:
21. Find if an array contains any duplicate values (return True/False)
22. Find the number that appears only once in an array where every other number appears twice (try without extra data structures first, then look up XOR trick after)
Rotation:
23. Rotate an array by 1 position to the right (e.g. [1,2,3,4,5] → [5,1,2,3,4])